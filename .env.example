REACTOR_API_KEY=replace_with_your_reactor_api_key
# Used by the AI director (terrain -> Orbis prompt) and the Nano Banana example.
GEMINI_API_KEY=replace_with_your_gemini_api_key
# Optional override for the AI director model (defaults to a free-tier text model):
# GEMINI_MODEL=gemini-3.5-flash
